{
    "name": "jugamir",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"docker compose up --build backend db pgadmin\" \"npm run dev --prefix frontend\"",
        "backend": "docker compose up backend db",
        "frontend": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}